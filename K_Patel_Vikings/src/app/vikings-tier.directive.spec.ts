import { VikingsTierDirective } from './vikings-tier.directive';

describe('VikingsTierDirective', () => {
  it('should create an instance', () => {
    const directive = new VikingsTierDirective();
    expect(directive).toBeTruthy();
  });
});
